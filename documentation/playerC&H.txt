typedef struct Player{
    char* name;
    int chips;
    int bet;
    int folded; //0-active, 1-folded
    int score;  //used for scoring
    int active; //1-active, 0-busted(chips = 0)
} Player;

void initPlayer(Player *p, int startingChips, char* name){...}
    Inicializira igralca, dodeli vse vrednosti

void DebugPrintPlayer(Player *p){...}
    Redundant DEBUG funkcija

void playerInitBet(Player *p, Board *b){...}
    Inicializira bet

int playerAction(){...}
    Igralca prompta da naredi potezo, pričakuje input celega števila 0,1,2,3

void Player_Call_Check(Player *p, Board *b){...}
    Call/Check action

void Player_Raise(Player *p,Board *b){...}
    Raise action

void Player_ALLIN(Player *p, Board *b){...}
    All-in action

void Player_Fold(Player *p,Board *b){...}
    Fold action

void PlayerActionExec(int choice, Player *p, Board *b){...}
    Pričakuje nek int choice, ki mu pred klicom funkcije dodelimo vrednost z playerAction()
    Glede na ta input, se izvrši želeni action

void PlayerSetScore(Player *p, int scoreInput){...}
    Zapis score-a pri evaluation