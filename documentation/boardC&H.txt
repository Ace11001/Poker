typedef struct Board{
    int pot;                  Total amount of staked chips
    int minBet;               Minimal amount of chips you have to bet to stay in the round
    int phase;                0-preflop|1-flop|2-turn|3-river
    int AllInStatus;          0-not active 1-active
    int AllInSize;            Size of All-In bet
    int communityCount;       How many community cards does the Hand Struct alternative poses - used for board texture
}Board;

void initBoard(Board *b){...}
    Initializes the Board struct

void payOutPot(Board *b, Player *p){...}
    Pays out the pot chips from board struct to player struct
void placeInPot(Player *p, Board *b){...}
    Pays in chips into pot from player struct into board struct
    
void bustDetection(Player *p){...}
    Old bustDetection -> update or remove

int getShowdown(int scores[],int playerCount){...}
    Old showdown -> update or remove