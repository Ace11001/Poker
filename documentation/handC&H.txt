typedef struct {
    Card cards[MAX_HAND];
    int count;
} Hand;

void initHand(Hand* hand){...}
    Inicializira deck in poda vrednosti Hand.count = 0;
    Deck Inicializiramo pred card-dealom!

void dealToHand(Hand* hand, Card* deck, int* deck_top) {...}
    Dodeli 1 karto danemo Hand structu

void printHand(Hand* hand){...}
    V terminal izpiše katere karte ima dani Hand struct